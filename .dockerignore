# Ignore everything by default
**

# Allow only what we need for the lab (if we were to build)
!docker-compose.yml
!scripts/

# Explicitly ignore secrets
.env
.env.example
.git/
.gitignore
README.md
LEHRER.md
FEHLERSUCHE.md
